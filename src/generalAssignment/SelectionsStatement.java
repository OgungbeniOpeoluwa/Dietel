package generalAssignment;

public class SelectionsStatement {
    public static void main(String[] args){
       for(int number = 1; number <= 12; number++){
           int sum = 24 ;
           int result = 50;
           int result2 = 29;
           int total =  sum * number;
           int total2 = result * number;
           int total3 = result2 * number;
           System.out.println(number + " * "+ sum + " = "+ total + " \t  " + number + " * " + result + " =" + total2 + "\t" + number + " * " + result2 + " =" + total3);
           //System.out.println(total);
       }
       }
    }